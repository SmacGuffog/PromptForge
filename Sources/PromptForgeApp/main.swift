// Placeholder entry point.
//
// The real menu-bar app (app shell, global hotkey, capture window, tabs) is
// built in Phase 8 onward of the implementation plan. This stub exists only so
// the executable target compiles while the portable core is developed against
// the `RewriteEngine` seam.
print("PromptForge: the app shell is not built yet. See docs/plans for the build order.")

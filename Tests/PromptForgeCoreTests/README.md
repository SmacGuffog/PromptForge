# PromptForgeCoreTests

Unit tests for the portable core. Because the core has no UI, its behaviour is testable in isolation: guide loading and saving, meta-prompt assembly, history append and read, and engine selection behind a fake `RewriteEngine`.

Populated alongside the core in the implementation plan.
